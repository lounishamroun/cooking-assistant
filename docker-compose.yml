version: "3.9"
services:
  web:
    build: .
    container_name: cooking-assistant-web
    ports:
      - "8501:8501"
    environment:
      - STRICT_REAL_DATA=1
    volumes:
      - ./data:/app/data:ro
    restart: unless-stopped
