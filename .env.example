# Example environment variables for Cooking Assistant
# Copy to .env and adjust as needed.

# Enforce only real interaction-derived metrics (no synthetic fallbacks)
STRICT_REAL_DATA=1

# Root data directory (used by loaders/processors)
DATA_PATH=./data

# Optional: override Streamlit server port
# STREAMLIT_SERVER_PORT=8501
